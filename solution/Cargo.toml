[package]
name = "in_memory_task_queue"
version = "0.1.0"
edition = "2021"

# The queue itself uses only the standard library.
[dependencies]

[lib]
name = "in_memory_task_queue"
path = "src/lib.rs"

[[bin]]
name = "demo"
path = "src/main.rs"

[profile.release]
opt-level = 2
